version: '3'

services:
    newrelic:
        container_name: test-project-newrelic
        image: newrelic/php-daemon
        networks:
            - main
    database:
        image: mysql:8.0
        command: --default-authentication-plugin=mysql_native_password
        environment:
            MYSQL_ROOT_PASSWORD: secret
            MYSQL_DATABASE: todolist
            MYSQL_USER: symfony
            MYSQL_PASSWORD: symfony
        ports:
            - '4306:3306'
        networks:
            - main
    php:
        build:
            context: ./docker
            args:
                NEW_RELIC_LICENSE_KEY: eu01xxe7962bded42fd3dc8737dcd9df185cNRAL
                NEWRELIC_APP_NAME: "TODO"
                NEWRELIC_DAEMON_ADDRESS: "newrelic:31339"
        volumes:
          - .:/var/www/projet8-todolist
        depends_on:
          - database
        networks:
            - main
    nginx:
        image: nginx:stable-alpine
        ports:
            - '8080:80'
        volumes:
            - .:/var/www/projet8-todolist
            - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
        depends_on:
            - php
            - database
        networks:
            - main

networks:
    main: